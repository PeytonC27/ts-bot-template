"use strict";
// import requests
// class JsonFetcher {
//     #url: string;
//     constructor(url: string) {
//         this.#url = url;
//     }
//     request(params: object) {
//         return this.fetch(this.#url, params);
//     }
//     fetch(data: string): void {
//         let fetchResult = fetch(this.#url.concat(this.#formatText(data)));
//         fetchResult.then( res => res.json() ).then( d => console.log(d) );
//     }
//     #formatText(str: string): string {
//         let newText = str.replaceAll(" ", "-").toLowerCase();
//         return newText
//     }
// }
// export = JsonFetcher;
